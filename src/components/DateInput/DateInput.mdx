As with every other form component, the DateInput is a controlled component and so it
requires for you to specify the `date` and the `onChangeDate` handler.

Any extra prop that you specify will be given to the underlying input element of the module.

---

```typescript jsx
const DateInputExample = () => {
  const [date, setDate] = React.useState();
  return (
    <Box>
      <Box mb={4}>Selected date is: {date ? date.toLocaleDateString() : '-'}</Box>
      <Box>
        <DateInput onChange={setDate} value={date} placeholder="Enter a date" label="Start date" />
      </Box>
    </Box>
  );
};

<DateInputExample />;
```

It support a "solid" color mode

```typescript jsx
const DateInputExample = () => {
  const [date, setDate] = React.useState();
  return (
    <Box>
      <Box>
        <DateInput
          variant="solid"
          onChange={setDate}
          value={date}
          placeholder="Enter a date"
          label="Start date"
        />
      </Box>
    </Box>
  );
};

<DateInputExample />;
```

It also supports manipulating the given time

```typescript jsx
const DateInputExampleWithTime = () => {
  const [date, setDate] = React.useState(new Date());
  return (
    <Box>
      <Box mb={4}>
        Selected date is {date.toLocaleDateString()} {date.toLocaleTimeString()}
      </Box>
      <Box>
        <DateInput
          withTime
          format="MM/DD/YYYY hh:mm A"
          onChange={setDate}
          value={date}
          placeholder="Enter a date"
          label="Start date"
        />
      </Box>
    </Box>
  );
};

<DateInputExampleWithTime />;
```

It supports alignment options.

```typescript jsx
const DateInputExampleWithTime = () => {
  const [date, setDate] = React.useState(new Date());
  return (
    <Box>
      <DateInput
        alignment="left"
        withTime
        mode="12h"
        format="MM/DD/YYYY HH:mm A"
        onChange={setDate}
        value={date}
        placeholder="Enter a date"
        label="Start date"
      />
    </Box>
  );
};

<DateInputExampleWithTime />;
```

It supports manipulating the given time in 24h format

```typescript jsx
const DateInputExampleWithTime = () => {
  const [date, setDate] = React.useState(new Date());
  return (
    <Box>
      <Box mb={4}>
        Selected date is {date.toLocaleDateString()} {date.toLocaleTimeString()}
      </Box>
      <Box>
        <DateInput
          withTime
          mode="24h"
          format="MM/DD/YYYY HH:mm A"
          onChange={setDate}
          value={date}
          placeholder="Enter a date"
          label="Start date"
        />
      </Box>
    </Box>
  );
};

<DateInputExampleWithTime />;
```

It supports "utc" timezone

```typescript jsx
const DateInputExample = () => {
  const [date, setDate] = React.useState();
  return (
    <Box>
      <Box mb={4}>Selected date is {date ? date.toISOString() : ''}</Box>
      <Box>
        <DateInput
          timezone="utc"
          withTime
          variant="solid"
          onChange={setDate}
          value={date}
          format="MM/DD/YYYY HH:mm A"
          placeholder="Enter a date"
          label="Start date"
        />
      </Box>
    </Box>
  );
};

<DateInputExample />;
```
