The fade can occur without any movement or witht a movement from top, left, right or bottom:

```typescript jsx
const Example = () => {
  const [_, forceRerender] = React.useState();
  return (
    <Box>
      <Box mb={5}>
        <Button onClick={() => forceRerender(Math.random())}>Force Rerender</Button>
      </Box>
      <SimpleGrid columns={5} gridGap={5} key={Math.random()}>
        <FadeIn>
          <Card p={3}>Center</Card>
        </FadeIn>
        <FadeIn direction="top">
          <Card p={3}>Top</Card>
        </FadeIn>
        <FadeIn direction="right">
          <Card p={3}>Right</Card>
        </FadeIn>
        <FadeIn direction="bottom">
          <Card p={3}>Bottom</Card>
        </FadeIn>
        <FadeIn direction="left">
          <Card p={3}>Left</Card>
        </FadeIn>
      </SimpleGrid>
    </Box>
  );
};

<Example />;
```

The fade can occur with different durations:

```typescript jsx
const Example = () => {
  const [_, forceRerender] = React.useState();
  return (
    <Box>
      <Box mb={5}>
        <Button onClick={() => forceRerender(Math.random())}>Force Rerender</Button>
      </Box>
      <SimpleGrid columns={5} gridGap={5} key={Math.random()}>
        <FadeIn duration={1000}>
          <Card p={3}>Center</Card>
        </FadeIn>
        <FadeIn duration={2000} direction="top">
          <Card p={3}>Top</Card>
        </FadeIn>
        <FadeIn duration={3000} direction="right">
          <Card p={3}>Right</Card>
        </FadeIn>
        <FadeIn duration={4000} direction="bottom">
          <Card p={3}>Bottom</Card>
        </FadeIn>
        <FadeIn duration={5000} direction="left">
          <Card p={3}>Left</Card>
        </FadeIn>
      </SimpleGrid>
    </Box>
  );
};

<Example />;
```

The fade can occur with different delays:

```typescript jsx
const Example = () => {
  const [_, forceRerender] = React.useState();
  return (
    <Box>
      <Box mb={5}>
        <Button onClick={() => forceRerender(Math.random())}>Force Rerender</Button>
      </Box>
      <SimpleGrid columns={5} gridGap={5} key={Math.random()}>
        <FadeIn delay={1000}>
          <Card p={3}>Center</Card>
        </FadeIn>
        <FadeIn delay={2000} direction="top">
          <Card p={3}>Top</Card>
        </FadeIn>
        <FadeIn delay={3000} direction="right">
          <Card p={3}>Right</Card>
        </FadeIn>
        <FadeIn delay={4000} direction="bottom">
          <Card p={3}>Bottom</Card>
        </FadeIn>
        <FadeIn delay={5000} direction="left">
          <Card p={3}>Left</Card>
        </FadeIn>
      </SimpleGrid>
    </Box>
  );
};

<Example />;
```

The fade can occur with different offsets:

```typescript jsx
const Example = () => {
  const [_, forceRerender] = React.useState();
  return (
    <Box>
      <Box mb={5}>
        <Button onClick={() => forceRerender(Math.random())}>Force Rerender</Button>
      </Box>
      <SimpleGrid columns={5} gridGap={5} key={Math.random()}>
        <FadeIn>
          <Card p={3}>Center</Card>
        </FadeIn>
        <FadeIn direction="top" offset={50}>
          <Card p={3}>Top</Card>
        </FadeIn>
        <FadeIn direction="right" offset={100}>
          <Card p={3}>Right</Card>
        </FadeIn>
        <FadeIn direction="bottom" offset={150}>
          <Card p={3}>Bottom</Card>
        </FadeIn>
        <FadeIn direction="left" offset={1200}>
          <Card p={3}>Left</Card>
        </FadeIn>
      </SimpleGrid>
    </Box>
  );
};

<Example />;
```
