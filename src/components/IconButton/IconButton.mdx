An Iconbutton can be solid:

```typescript jsx
<SimpleGrid columns={2} gap={4}>
  <IconButton icon="user" aria-label="User" />
  <IconButton icon="user" color="teal" aria-label="User" />
</SimpleGrid>
```

An IconButton can be ghost:

```typescript jsx
<SimpleGrid columns={2} gap={4}>
  <IconButton variant="ghost" icon="user" aria-label="User" />
  <IconButton variant="ghost" icon="user" aria-label="User" active />
</SimpleGrid>
```
