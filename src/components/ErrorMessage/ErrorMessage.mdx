An error message is used in any form element in order to denote an error

```typescript jsx
const Example = () => {
  return (
    <Box backgroundColor="navyblue-600" width="100%" height={200}>
      <ErrorMessage>You have a problem</ErrorMessage>
    </Box>
  );
};

<Example />;
```
